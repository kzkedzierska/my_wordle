guess_word <- function(words_df,
                       previous_guess,
                       regexpr_word,
                       regexpr_letters) {
  
}

guess_first <- function(words_for_wordle) {
  
}